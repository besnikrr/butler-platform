describe("Logger Service", () => {
  beforeAll(async () => {
    // init db...
  });

  it("should pass", async () => {
    expect(1 + 1).toEqual(2);
  });

  afterAll(async () => {
    // close connection
  });
});
